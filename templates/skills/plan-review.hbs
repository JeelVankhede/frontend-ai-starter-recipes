---
description: Architecture / UX plan review before implementation (no code)
---

# Skill: Plan Review

Use this **before** writing substantial UI or state changes for **{{projectName}}**.

Start with `.ai/lifecycle/think.md`, then check the proposed plan against `.ai/lifecycle/plan.md`.

## Checklist

1. **Problem & scope** — user story, acceptance criteria, out-of-scope items.
2. **UI decomposition** — components affected (`{{componentDir}}`), new vs reused.
3. **State & data** — what lives in {{stateManagement}} / {{dataFetching}}; cache keys if using TanStack/SWR/Apollo.
4. **Routing** — routes touched (`{{pagesDir}}`), deep links, guards/middleware.
5. **A11y** — focus order, keyboard path, live regions for async feedback.
6. **Responsive** — breakpoints, overflow, touch targets.
7. **Performance** — lazy boundaries, images, bundle impact of new deps.
8. **Tests** — unit/component cases; E2E smoke if critical path.
9. **Adapter/generated output** — if `.ai/` templates change, name adapter files to review.

## Stop Conditions

- The plan changes public routing, auth/session behavior, or shared state without naming user impact.
- The plan adds dependencies without approval.
- Tests or accessibility checks are missing for changed interactive behavior.

## Output

- **Approve** with summarized decisions, file impact, risks, and validation path.
- **Request changes** with concrete gaps and the lifecycle stage that must be revisited.
