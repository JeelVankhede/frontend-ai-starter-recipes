---
description: Quality pass — build, test, a11y, and basic perf signals
---

# Skill: QA

Run the full local quality gate for **{{projectName}}**.

Use `.ai/lifecycle/test.md` to choose checks proportional to the changed user flow.

## Steps

1. `{{buildCommand}}`
2. `{{lintCommand}}`
3. `{{testCommand}}`
{{#if (neq e2eCommand "")}}
4. `{{e2eCommand}}` (smoke subset if slow)
{{/if}}
5. **Accessibility** — run axe (browser extension or CI) on primary flows
6. **Lighthouse** (optional) — performance + a11y + best-practices on key URL
7. **Visual** — spot-check responsive breakpoints
8. **Generated output** — if `.ai/` templates changed, generate a sample project and review adapters

## Scorecard

| Check | Pass/Fail | Notes |
|-------|-----------|-------|
| Build | | |
| Lint | | |
| Unit tests | | |
{{#if (neq e2eCommand "")}}
| E2E | | |
{{/if}}
| Axe | | |
| Lighthouse | | |
| Generated output / adapters | | |

## Output

- Commands run and results.
- Manual QA notes.
- Untested areas and residual risk.
