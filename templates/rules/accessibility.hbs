---
description: Accessibility (a11y) baseline for UI work
---

# Accessibility

Use this rule with `.ai/lifecycle/think.md`, `.ai/lifecycle/plan.md`, `.ai/lifecycle/review.md`, and `.ai/lifecycle/test.md`.

## Baseline

- Meet **WCAG 2.1 AA** for primary user journeys where feasible.
- Prefer **semantic HTML** (`button`, `nav`, `main`, `header`, `section`) over div-only layouts.

## Keyboard

- All interactive controls reachable via **Tab**; visible **focus rings** (do not remove without replacement).
- Modals trap focus and return focus on close.

## ARIA

- Use ARIA only when semantics are insufficient; prefer native elements first.

## Color & motion

- Text contrast **≥ 4.5:1** for body copy; test states (hover, disabled, error).
- Honor `prefers-reduced-motion`.

## Testing

- Complement automated checks (axe) with **keyboard-only** passes for new flows.

## Plan Requirements

- Name affected interactive controls and expected keyboard path.
- Define focus behavior for dialogs, popovers, route transitions, and error states.
- Identify async feedback that needs live regions or clear visible messaging.

## Must Avoid

- ARIA as a substitute for native semantics.
- Icon-only buttons without accessible labels.
- Color-only status or validation cues.
- Modal/popover changes without focus management review.

## Acceptance Criteria

- Primary flows are keyboard reachable.
- Form and async errors are perceivable.
- Labels, roles, focus order, and contrast are checked manually or with tooling.
